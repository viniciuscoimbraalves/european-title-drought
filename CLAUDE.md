# European Title Drought — CLAUDE.md

## Project
Website tracking how long it's been since 23 European clubs last won the UEFA Champions League. Real-time counter with years, months, days, hours, minutes, and seconds.

**URL**: https://www.european-title-drought.vercel.app (TBD)
**Stack**: React 18 + TypeScript + Vite + TanStack Router v1 + Tailwind CSS v4 + Vercel

## Commands
- `npm run dev` — local dev server
- `npm run build` — production build
- `npm run preview` — preview production build
- `npm run format` — Prettier
- `npm run lint` — ESLint
- `npx vercel --prod` — manual production deploy

## Structure
```
src/
├── components/       # AppShell, Logo, ShareButtons, BiggestDroughts
├── data/
│   └── teams.ts      # 23 clubs with colors, crests, title dates, COMPETITIONS array
├── lib/
│   ├── drought.ts    # calculateDrought(), formatDrought(), formatDroughtFull()
│   ├── slug.ts       # slugify()
│   └── utils.ts      # cn(), formatDate()
├── routes/
│   ├── __root.tsx            # root layout with AppShell
│   ├── index.tsx             # home with BiggestDroughts + team grid
│   ├── team.$slug.index.tsx  # team page (competition list)
│   ├── team.$slug.$competition.tsx  # competition detail
│   ├── about.tsx
│   ├── criteria.tsx
│   └── privacy.tsx
├── styles.css        # blue/purple design system (oklch), dark mode, font-timer
├── routeTree.gen.ts  # auto-generated by TanStack Router
└── main.tsx
```

## Clubs (23)
AC Milan, Ajax, Arsenal, Aston Villa, Barcelona, Bayern Munich, Benfica, Chelsea, Borussia Dortmund, PSV Eindhoven, Fenerbahçe, Feyenoord, Hamburg, Inter Milan, Juventus, Liverpool, Manchester City, Manchester United, Marseille, Nottingham Forest, Porto, PSG, Real Madrid, Red Star Belgrade, Steaua București.

## Competitions (single for now, extensible)
- champions-league — UEFA Champions League / European Cup

Add new competitions via `COMPETITIONS` array and `Record<string, string>` titles in `teams.ts`.

## Business rules
- Dates with year ≤ 1900 are placeholders (club never won that competition) — filtered in components.
- `calculateDrought()` receives ISO date + optional Date ref, returns `Drought` with years/months/days/hours/minutes/seconds/totalDays/totalSeconds.
- Timer updates every 1 second via `setInterval` + `useState<Date | null>` for correct hydration.
- `localStorage.getItem("european-title-drought-theme")` controls light/dark theme with `.dark` class on `<html>`.

## Design System
- **Colors**: oklch (`src/styles.css` lines 64-141), primary ~blue (260 hue), secondary ~purple (300 hue)
- **Gradients**: `--gradient-hero` (blue-purple) used in logo and page headers
- **Animations**: Tailwind transitions, `tw-animate-css`
- **Icons**: `lucide-react`

## Logo
- Inline SVG in `src/components/Logo.tsx` with `currentColor` and `--gradient-hero`
- Trophy/star icon + "European Title Drought" text
- No external images, no HTTP requests, no theme prop

## Code patterns
- Routes: TanStack Router v1 with `createFileRoute` + `Route.useParams()`
- Style: Tailwind CSS v4 with `@theme inline`, `@custom-variant dark`
- Fonts: system-ui, tabular-nums via `.font-timer` class
- Components exported as `export function Name()`
- CSS variables with `oklch()`, no hex or rgb
- Foundation and title dates in ISO format (YYYY-MM-DD)

## Deployment Flow
1. **Local** — edit, test (`npm run dev`, `npm run build`)
2. **Commit + Push** to GitHub — `git add` + `git commit` + `git push`
3. **Deploy** — `npx vercel --prod`

Never deploy without committing and pushing first. The three environments must be kept identical at all times.

## Remotes
- GitHub: TBD (needs remote setup)
- Vercel: TBD (needs project creation and deployment)
